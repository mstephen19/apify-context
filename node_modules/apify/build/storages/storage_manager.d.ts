/**
 * StorageManager takes care of opening remote or local storages.
 * @template T
 * @property {Function} StorageConstructor
 * @property {string} name
 * @property {LruCache} cache
 * @ignore
 */
export class StorageManager<T> {
    /**
     * @param {T} StorageConstructor
     * @param {Configuration} [config]
     */
    constructor(StorageConstructor: T, config?: Configuration | undefined);
    StorageConstructor: T;
    name: any;
    cache: import("@apify/datastructures").LruCache<any>;
    config: Configuration;
    /**
     * @param {string} [idOrName]
     * @param {object} [options]
     * @param {boolean} [options.forceCloud]
     * @return {Promise<T>}
     */
    openStorage(idOrName?: string | undefined, options?: {
        forceCloud?: boolean | undefined;
    } | undefined): Promise<T>;
    /**
     * @param {object} storage
     * @param {string} storage.id
     * @param {string} [storage.name]
     * @param {boolean} [storage.isLocal]
     */
    closeStorage(storage: {
        id: string;
        name?: string | undefined;
        isLocal?: boolean | undefined;
    }): void;
    /**
     * @param {string} idOrName
     * @param {boolean} isLocal
     * @return {string}
     * @ignore
     * @protected
     * @internal
     */
    protected _createCacheKey(idOrName: string, isLocal: boolean): string;
    /**
     * Helper function that first requests storage by ID and if storage doesn't exist then gets it by name.
     * @param {string} storageIdOrName
     * @param {string} storageConstructorName
     * @param {ApifyClient|ApifyStorageLocal} apiClient
     * @ignore
     * @protected
     * @internal
     */
    protected _getOrCreateStorage(storageIdOrName: string, storageConstructorName: string, apiClient: ApifyClient | ApifyStorageLocal): Promise<any>;
    /**
     * @param {ApifyClient|ApifyStorageLocal} client
     * @param {string} storageConstructorName
     * @ignore
     * @protected
     * @internal
     */
    protected _getStorageClientFactories(client: ApifyClient | ApifyStorageLocal, storageConstructorName: string): {
        createStorageClient: any;
        createStorageCollectionClient: any;
    };
    /**
     * @param {object} storage
     * @param {string} storage.id
     * @param {string} [storage.name]
     * @param {boolean} [storage.isLocal]
     * @ignore
     * @protected
     * @internal
     */
    protected _addStorageToCache(storage: {
        id: string;
        name?: string | undefined;
        isLocal?: boolean | undefined;
    }): void;
}
import { Configuration } from "../configuration";
import { ApifyClient } from "apify-client/dist/apify_client";
import { ApifyStorageLocal } from "@apify/storage-local";
//# sourceMappingURL=storage_manager.d.ts.map